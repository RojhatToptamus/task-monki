Codex events are not human-readable — no need to dump all events. Add better logs where possible; if not feasible, keep the current behavior.

Above the "Create Task" form, add a Refine Prompt button. The user can write something short and the button converts it into a well-structured prompt aligned with the repository's codebase. Generation may take a moment — don't over-invest time in it.

Heavily leverage global CSS custom properties (tokens, theme variables, etc.). Refactor toward a clean, modern macOS-style design.